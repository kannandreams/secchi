[projects.demo]
description = "Secchi demo — a mixed portfolio across registries"
favorite = true
packages = [
    { name = "serde", registry = "crates.io" },
    { name = "ripgrep", registry = "crates.io" },
    { name = "textual", registry = "pypi" },
    { name = "httpx", registry = "pypi" },
    { name = "rich", registry = "pypi" },
    { name = "tokio", registry = "crates.io" },
    { name = "clap", registry = "crates.io" },
    { name = "react", registry = "npm" },
    { name = "express", registry = "npm" },
    { name = "typescript", registry = "npm" },
]

[projects.duckdb]
description = "DuckDB — embeddable analytical database"
favorite = true
repository = "https://github.com/duckdb/duckdb"
packages = [
    { name = "duckdb", registry = "pypi" },
    { name = "duckdb", registry = "npm" },
]

[projects.opencode]
description = "OpenCode CLI — AI coding agent"
packages = [
    { name = "opencode", registry = "crates.io" },
]

[projects.tuffcli]
description = "tuffcli — capability lifecycle manager for coding agents"
favorite = true
packages = [
    { name = "tuffcli", registry = "crates.io" },
    { name = "tuffcli", registry = "pypi" },
]

[projects.textual]
description = "Textual — Python TUI framework"
packages = [
    { name = "textual", registry = "pypi" },
]

[projects.react]
description = "React — UI library"
packages = [
    { name = "react", registry = "npm" },
]

[projects.serde]
description = "Serde — Rust serialization framework"
packages = [
    { name = "serde", registry = "crates.io" },
]

[projects.httpx]
description = "HTTPX — Python HTTP client"
packages = [
    { name = "httpx", registry = "pypi" },
]
